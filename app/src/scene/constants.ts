

/** World units (e.g. metres). Long touchlines run along ±X; goal lines at ±Z. */
export const FIELD_WORLD_WIDTH = 120;
export const FIELD_WORLD_DEPTH = 60;
/** Playable area inside the outer touch/goal lines, used for formation placement. */
export const FIELD_WORLD_INNER_WIDTH = 96;
export const FIELD_WORLD_INNER_DEPTH = 60;

/** Centre circle radius (FIFA ~9.15m), in world units */
export const CENTER_CIRCLE_RADIUS = 9.15;
